[package]
name = "parser"
version = "0.1.0"
edition = "2024"

[dependencies]
quick-xml = { version = "0.42.0", features = ["serde", "serialize"] }
chrono = "0.4.45"
serde = { version = "1.0.229", features = ["derive"] }
